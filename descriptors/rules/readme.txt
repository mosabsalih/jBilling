The rules need to be in binary format for the junit test to work. In this directory there's both the source and binary. 
To compile the source, use the drools gui. Idealy, the ant task would do this, but it did not work.
